{
  "name": "blinksend",
  "version": "0.4.0-beta.1",
  "private": true,
  "type": "module",
  "description": "Direct browser-to-browser file sharing",
  "scripts": {
    "start": "node server.js",
    "test": "node scripts/run-tests.mjs",
    "bench": "node scripts/benchmark.mjs",
    "loadtest": "node scripts/load-benchmark.mjs"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "qrcode": "^1.5.4",
    "ws": "^8.18.3"
  }
}
